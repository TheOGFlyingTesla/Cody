interface:
  display_name: "Cody — Codex Coordinator"
  short_description: "Coordinate, route, and recover project work"
  default_prompt: "Make this task the repository's durable Cody coordinator. Inspect and reconcile first, then state the next verified action and route bounded work economically."

policy:
  allow_implicit_invocation: false
