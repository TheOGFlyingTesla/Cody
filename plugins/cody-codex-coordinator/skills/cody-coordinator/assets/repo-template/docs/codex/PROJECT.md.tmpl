# $PROJECT_SLUG — Project Brief

## Purpose

Unknown — inspect before relying on this field.

## Current product shape

Unknown — inspect before relying on this field.

## Repository map

- Repository root: current checkout.
- Modules and ownership boundaries: Unknown — inspect before relying on this field.

## Technology and runtime

Unknown — inspect before relying on this field.

## Validation commands

$VALIDATION_COMMANDS

## Generated files and ownership boundaries

- Coordinator-owned: the managed block in `AGENTS.md` and `docs/codex/`.
- Product generated-file boundaries: Unknown — inspect before relying on this field.

## Deployment shape

Unknown — inspect before relying on this field. Do not record credentials here.

## Authority and risk

$RISK_RULES

Commit, push, merge, deploy, secret, provider, billing, production, trading, and real-recipient actions require their own explicit authority.

## External systems

Unknown — inspect before relying on this field. Record names only, never credential values.

## Unknowns

- Project purpose, module map, deployment path, and generated boundaries require evidence-based discovery.
